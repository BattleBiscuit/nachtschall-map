version: '3.8'

services:
  nachtschall-map:
    build: .
    image: nachtschall-map:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - proxy
      - internal
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nachtschall-map.rule=Host(`nachtschall-map.seidel.life`)"
      - "traefik.http.routers.nachtschall-map.entrypoints=web"
      - "traefik.http.routers.nachtschall-map.tls.certresolver=le"
      - "traefik.http.services.nachtschall-map.loadbalancer.server.port=3000"

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    networks:
      - internal
    volumes:
      - redis-data:/data

networks:
  proxy:
    name: proxy
  internal:

volumes:
  redis-data:
